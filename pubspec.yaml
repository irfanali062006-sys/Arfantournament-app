name: tournament_app
description: FF Tournament App by Erfan Ale
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0 # Firebase se baat karne ke liye zaruri hai

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
